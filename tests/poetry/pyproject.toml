[project]
name = "test"
version = "0.1.0"
description = ""
requires-python = ">=3.13"
dependencies = [
    "pytest (>=9.0.3,<10.0.0)",
    "black (==26.5.1)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
